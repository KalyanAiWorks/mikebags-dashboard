services:
  - type: web
    name: mikebags-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: node api/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
    envVarsFromGroups:
      - group: mikebags-prod
